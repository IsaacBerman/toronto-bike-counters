// Reuse the parent segment's Open Graph image for city deep links so shared
// /downtown-definer/<city> URLs unfurl with the same branded card.
export { default, alt, size, contentType } from '../opengraph-image';
